spring.application.name=livraria-unipar
#caminho do banco de dados: local + porta + nome da database
#database deve ser criada manualmente no Postgres antes de rodar a aplicação
spring.datasource.url=jdbc:postgresql://localhost:5434/livraria
spring.datasource.username=postgres
spring.datasource.password=cagema120x90
spring.datasource.driver-class-name=org.postgresql.Driver

# auxilia o dev a ver os comandos SQL que o JPA está gerando
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# gera automatico as tabelas e colunas no banco de dados
spring.jpa.hibernate.ddl-auto=update